# Shopify cart and checkout fixtures

These fixtures are synthetic and redacted. They contain no buyer identity, payment data, credentials,
real merchant domains, reusable tokens, or production resource IDs. They exercise full-state cart
replacement, unknown checkout extensions/protocol metadata, and malformed-response rejection.
