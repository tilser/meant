package com.meant.api.module.user.constant;

/** Exact user-answerable decision addressed by a qualification follow-up. */
public enum UserProductSearchQuestionTarget {
    CONDITION,
    SHIPS_TO,
    SHIPS_FROM,
    PRICE,
    COLOR,
    SIZE,
    TARGET_GENDER,
    RATING,
    PRICE_TIER
}
