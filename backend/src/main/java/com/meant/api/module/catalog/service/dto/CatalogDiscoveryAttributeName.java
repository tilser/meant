package com.meant.api.module.catalog.service.dto;

/** Provider-neutral attribute names that a discovery source can translate to its catalog vocabulary. */
public enum CatalogDiscoveryAttributeName {
    COLOR,
    SIZE,
    TARGET_GENDER
}
